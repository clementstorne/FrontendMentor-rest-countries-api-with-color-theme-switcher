import { describe, expect, it } from "vitest";
// import mapCountryCodesToNames from "../mapCountryCodesToNames";

describe("mapCountryCodesToNames", () => {
  //   it("should return an array of country names for valid country codes", () => {
  //     const countryCodes = ["USA", "CAN", "GBR"];
  //     const countryNames = mapCountryCodesToNames(countryCodes);

  //     expect(countryNames).toEqual(["United States", "Canada", "United Kingdom"]);
  //   });

  //   it("should handle an empty input array", () => {
  //     const countryCodes: string[] = [];
  //     const countryNames = mapCountryCodesToNames(countryCodes);

  //     expect(countryNames).toEqual([]);
  //   });

  it("simple test", () => {
    expect(1 + 2).toEqual(3);
  });
});
